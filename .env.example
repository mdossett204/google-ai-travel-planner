# Server-side API keys used by the Vercel functions in /api.
# These are not exposed to the browser.

# Required: used by the recommendations route, itinerary draft stage,
# and as one of the supported itinerary verification providers.
GEMINI_API_KEY="YOUR_GEMINI_API_KEY"

# Required: one of the supported itinerary verification providers.
ANTHROPIC_API_KEY="YOUR_ANTHROPIC_API_KEY"

# Required: used to verify hotels, restaurants, and attractions by place search.
TOMTOM_SEARCH_API_KEY="YOUR_TOMTOM_SEARCH_API_KEY"

# Required: one of the supported itinerary verification providers.
OPENAI_API_KEY="YOUR_OPENAI_API_KEY"

# Required for caching and server-side rate limiting.
REDIS_URL="YOUR_REDIS_URL"

# Optional: choose which provider handles final itinerary verification.
# Supported values: gemini, anthropic, openai.
ITINERARY_VERIFICATION_PROVIDER="openai"

# Optional: override the verification model used for the provider above.
ITINERARY_VERIFICATION_MODEL=""

# Optional: controls the default provider in api/utils/llmRouter.ts when a request
# does not explicitly set one. Supported values: gemini, anthropic, openai.
DEFAULT_LLM_PROVIDER="gemini"

# Optional: enables verbose server-side logging for LLM/tool routing and TomTom search.
DEBUG_LLM_ROUTER="false"
